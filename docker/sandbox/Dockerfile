# Vuhitra Sandbox - Dockerfile (Placeholder)
# This will be implemented in Step 8: Sandbox Service & File Handling

# TODO: Implement sandbox service with:
# - Alpine Linux base
# - Basic utilities (bash, curl)
# - Shared volume with CLI service
# - Automatic cleanup mechanisms

# Placeholder
FROM alpine:latest

RUN apk add --no-cache \
    bash \
    curl

WORKDIR /workspace

# Keep container running
CMD ["tail", "-f", "/dev/null"]
